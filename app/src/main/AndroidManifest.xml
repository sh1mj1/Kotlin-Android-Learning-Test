<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:allowBackup="true"
        android:name=".fourComponents.service.BaseApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearningTest"
        tools:targetApi="31">
        <activity
            android:name=".compose.basiclayout.SootheActivity"
            android:exported="true"
            android:label="@string/title_activity_soothe"
            android:theme="@style/Theme.LearningTest">

        </activity>

        <activity
            android:name=".compose.state.BasicStateActivity"
            android:exported="true"
            android:label="@string/title_activity_basic_state"
            android:theme="@style/Theme.LearningTest" />

        <activity
            android:name=".compose.basic.codelab.BasicComposeActivity"
            android:exported="true"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.LearningTest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".fourComponents.service.PlaybackBoundServiceActivity"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <service
            android:name=".fourComponents.service.PlaybackBoundService"
            android:exported="false" />

        <service
            android:name=".fourComponents.service.MusicPlayerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

    </application>

</manifest>